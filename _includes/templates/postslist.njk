<ul class="postList">
  {% for post in postslist %}
  <li class="postList-post">
    <a href="{{ post.url }}" class="postList-title"{% if entry.data.url == url %} aria-current="page"{% endif %}>
      {% if post.title %}
        <strong>{{ post.title }}</strong>
      {% else %}
        <code>{{ post.url }}</code>
      {% endif %}
    </a>

    <time class="postList-date" datetime="{{ post.data.date | date('DATETIME') }}">
      {{ post.date | date('HUMAN_DATE') }}
    </time>

    {% for tag in post.tags %}
    {% set page = search.page("type=tag tag='" + tag + "'") %}
    {% if page %}
    <a href="{{ page.url  }}" class="tag">{{ tag }}</a>
    {% endif %}
    {% endfor %}
  </li>
{% endfor %}
</ul>
